# aurora-routing-agent

AI agent that computes optimal delivery routes and dispatch decisions.

Mockup repository built to exercise Backstage's cataloging of an
Agent2Agent (A2A) capable agent — see the
[Agent Card](../.well-known/agent.json) for its (mock) skills.
